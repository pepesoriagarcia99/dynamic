import { Routes } from '@angular/router';

export const routes: Routes = [
    // { path: '', component: Home },
    // {
    //     path: 'creator',
    //     loadComponent: () => import('./modules/form-creator').then(m => m.FormCreator)
    // },
];
